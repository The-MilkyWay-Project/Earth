#Development
version: '3'
services:
    earth-development:
        build:
          context: .
          dockerfile: .Infrastructure/Docker/Development/Dockerfile
        ports:
          - "5000:5000"
        volumes:
          - ./App:/app
        command: npm install
        command: npm run dev
